import React from "react";
import "./App.css";
function App() {
  return (
    <>
      <h1>State in React </h1>
      <ul style={{ textAlign: "left" }}>
        <li>State is container to store data like variable.</li>
        <li>This is mutable and dynamic</li>
        <li>We have to import it when we have use it.</li>
        <li>
          it re-render components automatically so that data visible on ui.
        </li>
      </ul>
    </>
  );
}

export default App;
